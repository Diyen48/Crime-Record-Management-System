# Treat all files as text by default
* text=auto

# Ensure SQL and Markdown files use LF line endings and are treated as text
*.sql text eol=lf
*.md text eol=lf

# Ensure PDFs and images are treated as binary
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.svg binary

# Highlight SQL properly on GitHub
*.sql linguist-language=SQL
